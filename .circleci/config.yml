version: 2.1
jobs:
  deploy:
    docker:
      - image: circleci/yarn:lts
    steps:
      - checkout
      - run: yarn install
      - run: cd cf-worker
      - run: yarn add @cloudflare/wrangler -g; wrangler publish

workflows:
  deploy:
    jobs:
      - deploy:
          context: cf-worker
